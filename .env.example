# StreetEasy URL to scrape - customize with your search criteria
# Example: https://streeteasy.com/for-rent/nyc/price:-3000|beds%3C=2|area:106,117
SE_URL=https://streeteasy.com/for-rent/nyc

# Email configuration for notifications
# For Gmail: Use your Gmail address and generate an App Password
# (requires 2-factor authentication enabled)
EMAIL_ADDRESS=your_email@gmail.com
EMAIL_PASSWORD=your_16_character_app_password
TO_EMAIL=your_email@gmail.com

# Optional: Custom SMTP settings (defaults to Gmail)
# SMTP_SERVER=smtp.gmail.com
# SMTP_PORT=587

# Database path (optional, defaults to apartments.db)
DB_PATH=apartments.db
# Time in minutes that the scheduler should run
TIME_INTERVAL=5
# Check interval in minutes (default: 5)
TIME_INTERVAL=5
